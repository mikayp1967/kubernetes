apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pipeline-proj.fullname" . }}-test-connection"
  labels:
    {{- include "pipeline-proj.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pipeline-proj.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
