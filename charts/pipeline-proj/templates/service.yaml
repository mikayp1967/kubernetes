apiVersion: v1
kind: Service
metadata:
  name: {{ include "pipeline-proj.fullname" . }}
  labels:
    {{- include "pipeline-proj.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pipeline-proj.selectorLabels" . | nindent 4 }}
